{
  "Logging": { //Global Configuration for all providers
    "LogLevel": { // All providers, LogLevel applies to all the enabled providers.
      "Default": "Information", // Overrides preceding LogLevel:Default setting.
      "Microsoft.AspNetCore": "Warning" // All Microsoft* categories, Warning and higher.
    }
  },
  "Console": { // Console provider.
    "LogLevel": {
      "Default": "Error", // Overrides preceding LogLevel:Default setting.
      "Microsoft.Hosting": "Trace" // Debug:Microsoft.Hosting category.
    }
  },
  "Debug": { // Debug provider.
    "LogLevel": {
      "Default": "Trace", // Overrides preceding LogLevel:Default setting.
      "Microsoft.Hosting": "Error" // Debug:Microsoft.Hosting category.
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": { "CollegeAppDBConnection": "Data Source=JR\\SQLEXPRESS;Initial Catalog=CollegeAppDB;Integrated Security=True;Trust Server Certificate=True" }
}
  
